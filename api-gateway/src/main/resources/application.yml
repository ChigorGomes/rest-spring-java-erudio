spring:
  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/course_erudio?useTimezone=true&serverTimezone=UTC
      username: root
      password: root
  jpa:
    hibernate:
      ddl-auto: none #   não altera a estrutura do banco
    properties:
      hibernate:
    show_sql: false
    open-in-view: false

